.env
*.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/

# dbt (all generated/credential files now live under dbt/)
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
dbt/profiles.yml

# OS
.DS_Store
Thumbs.db

# Misc generated
logs/
dbt_internal_packages/


code_review.md

# Private keys — catch all formats, not just specific filenames
*.pem
*.p8

# dbt profiles (contains credentials — use env vars instead)
profiles.yml

# Airflow logs (generated at runtime)
airflow/logs/**
!airflow/logs/.gitkeep
# Backfill wave ticker list files (generated, not source-controlled)
scripts/backfill_wave*.txt
